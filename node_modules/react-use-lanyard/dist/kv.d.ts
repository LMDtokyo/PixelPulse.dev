import { IDelete, ISet } from "./types";
export declare const set: ({ apiKey, key, userId, value, apiUrl }: ISet) => Promise<Response>;
export declare const del: ({ apiKey, key, userId, apiUrl }: IDelete) => Promise<Response>;
