---
import Container from '@/components/Container.astro'
import { SOCIAL_LINKS } from '@/consts'
import SocialIcons from './SocialIcons.astro'
---

<footer class="relative py-8 bg-[#0d0d0d] text-gray-300 font-mono">
  <Container>
    <div class="flex flex-col items-center gap-8 sm:gap-10">

      <!-- Блок с текстом о любимой -->
      <div class="relative max-w-3xl w-full text-center">
        <!-- "рамка" окна в стиле терминала -->
        <div class="border border-gray-700 p-4 rounded-md relative">
          <!-- Заголовок "терминального окна" -->
          <div class="absolute -top-3 left-4 bg-[#0d0d0d] px-2 text-sm text-neon-green">
            memories.log
          </div>
          <p class="text-sm leading-relaxed text-gray-200 tracking-wide">
            <span class="text-neon-green">&gt;</span> К сожалению, я уже отдал свое сердце женщине. Хотя она меня не любит.<br/>
            <span class="text-neon-green">&gt;</span> Она потеряла тех, кого любила, одного за другим,<br/>
            <span class="text-neon-green">&gt;</span> так что теперь она не может любить.<br/>
            <span class="text-neon-green">&gt;</span> Я пообещал наблюдать за ней и смирился с ношей ее будущего на своих плечах.
          </p>
          <!-- Добавим лёгкий "глич"-эффект через псевдоэлементы -->
          <div class="pointer-events-none absolute inset-0 overflow-hidden">
            <div class="absolute inset-0 bg-transparent mix-blend-difference animate-glitch"></div>
          </div>
        </div>
      </div>

      <!-- Разделительная линия -->
      <div class="w-full border-t border-gray-700"></div>

      <!-- Копирайт и соц. иконки -->
      <div class="flex w-full flex-col items-center justify-between gap-4 sm:flex-row">
        <p class="text-xs sm:text-sm text-gray-500">
          &copy; {new Date().getFullYear()} PixelPulse.dev
        </p>
        <SocialIcons links={SOCIAL_LINKS} class="flex gap-4 text-gray-400 hover:text-neon-green transition-colors duration-200" />
      </div>
    </div>
  </Container>
</footer>

<style global>
    :root {
        /* Неоновый оттенок (зеленоватый): */
        --neon-green: #00ff7f;
    }

    .text-neon-green {
        color: var(--neon-green);
    }

    /* Глич-анимация */
    @keyframes glitch {
        0% { transform: translate(0, 0) }
        20% { transform: translate(-1px, -1px) }
        40% { transform: translate(1px, 1px) }
        60% { transform: translate(-2px, 1px) }
        80% { transform: translate(2px, -1px) }
        100% { transform: translate(0, 0) }
    }

    .animate-glitch {
        animation: glitch 500ms infinite ease-in-out alternate;
    }
</style>
